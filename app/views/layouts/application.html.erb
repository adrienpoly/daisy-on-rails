<!DOCTYPE html>
<html>
  <head>
    <%= render "layouts/head" %>
  </head>

  <body>
    <%= render "shared/navbar" %>
    <div class="pt-16">
      <%= yield %>
    </div>
    <%= render "shared/flashes" %>
    <%= turbo_stream_from Current.user if Current.user %>
  </body>
</html>
